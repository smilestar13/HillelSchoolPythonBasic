Ваша задача — написать функцию add_one, которая принимает список из цифр, которые являются одним числом,
к которому необходимо добавить к числу 1.

Т.е. Вам необходимо из набора цифр, входящего списка, получить число,
cложить его с 1, и полученную сумму, снова разбить на список из цифр.

В результате функция возвращает список из цифр, составляющих значение суммы.

Примеры:
add_one([1, 2, 3, 4]) возвращает [1, 2, 3, 5]
add_one([9, 9, 9, 9]) возвращает [1, 0, 0, 0, 0]
add_one([0]) возвращает [1]
add_one([9]) возвращает [1, 0]